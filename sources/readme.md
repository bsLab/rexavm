# Build

1. Extract sources zip archive
2. cd rexavm/build/c
3. make world

That's all. The reaxavm (just named `vm`) should be build after a few seconds. 

4. Start `vm` or execute a script `vm script.fth`

